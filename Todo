Install prompt
Auto launch preferred link into standalone app
Add clear/restart button
Fix the logo not being available offline
See if I can fetch the API calculation modules and then cache it for local use (so that I don't have to update the client repo with any API repo changes)

Can use css media queries:
@media (display-mode: browser){} - targets app within the browser only
@media (display-mode: standalone){} - targets app in standalone mode only

Add class .unselectable to non-user-selectable items to avoid ugly highlighting

Avoid accidental refresh
body {
overscroll-behaviour-y: contain;
}

Before launch

- restore commented out redirects to start of form if blank
- Remove joe bloggs
